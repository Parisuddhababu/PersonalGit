import React from 'react';
import { useTranslation } from 'react-i18next';

const Engagement = () => {
	const { t } = useTranslation();
	
	return (
		<div>
            {t('Engagement Page Incomming!!!')}
		</div>
	);
};
export default Engagement;
