import React from 'react';
import { useTranslation } from 'react-i18next';

const SmartLifters = () => {
	const { t } = useTranslation();
	
	return (
		<div>
            {t('Smart Lifters Page Incomming!!!')}
		</div>
	);
};
export default SmartLifters;
