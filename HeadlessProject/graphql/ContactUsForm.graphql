mutation ContactUsForm(
  $fullname: String!
  $email: String!
  $telephone: String!
  $message: String!
) {
  ContactUsForm(
    input: { fullname: $fullname, email: $email, telephone: $telephone, message: $message }
  ) {
    response
  }
}
